{% extends 'base.html.twig' %}

{% block title %}Ajouter module{% endblock %}

{% block body %}
<div class="login">
    {% if edit %}
    <h1>Editer un module</h1> 
    {% else %}
    <h1>Ajouter un module</h1>
    {% endif %}

<div class="list-form">

    {# formulaire ajout et edit module  #}
    {{ form_start(formAddSection) }}
    {{ form_row(formAddSection.nomSection) }}
    {{ form_row(formAddSection.categorie) }}
    {{ form_end(formAddSection) }}
</div>
</div>

    
{% endblock %}