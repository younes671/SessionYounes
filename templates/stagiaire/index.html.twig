{% extends 'base.html.twig' %}

{% block title %}Hello StagiaireController!{% endblock %}

{% block body %}
    {% if app.user %}
    <div class="login">
      <h1>Liste des stagiaires</h1>
          
      <a class="btn btn-primary" href="{{ path('new_stagiaire') }}">Ajouter un stagiaire</a><br>
    </div>


            {# affichage liste et gestion stagiaire  #}
            <div class="list">
                <table class="table-bordered">
                    <thead>
                      <tr>
                        <th class="th" scope="col">Nom</th>
                        <th class="th" scope="col">Prénom</th>
                        <th class="th" scope="col">Editer</th>
                        <th class="th" scope="col">Supprimer</th>
                      </tr>
                    </thead>
                    <tbody>
                    {% for stagiaire in stagiaires %}
                        <tr>
                          </th>
                          <td><a  href="{{ path('show_stagiaire', {'id': stagiaire.id}) }}">{{ stagiaire.nom }}</a></td>
                          <td><a  href="{{ path('show_stagiaire', {'id': stagiaire.id}) }}">{{ stagiaire.prenom }}</a></td>
                          <td><a  href="{{ path('edit_stagiaire', {'id': stagiaire.id}) }}"><i class="fa-solid fa-pen-to-square"></i></a></td>
                          <td><a  href="{{ path('delete_stagiaire', {'id': stagiaire.id}) }}"><i class="fa-solid fa-trash"></i></a></td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
        </div>
    {% else %}
        <meta http-equiv="refresh" content="0;url={{ path('app_login') }}">
{% endif %}
    


{% endblock %}
