<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
        <link rel="stylesheet" href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css' integrity='sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN' crossorigin='anonymous'>
        <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        {% block stylesheets %}
        {% endblock %}

        
    </head>
    <body>
        <div class="navbar navbar-expand-lg">
            {% if app.user %}
            <div class="container-fluid">
                        <a class="navbar-brand">
                            
                        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                                <span class="navbar-toggler-icon"></span>
                        </button>
                                
                        <div class="pseudo">
                            <p class="lien-nav"><i class="fa-solid fa-user"></i>{{ app.user.pseudo ~ ' '}}</p> 
                        </div>
                        </a>
                        <nav class="collapse navbar-collapse color-style" id="navbarNav">
                            <ul class="navbar-nav">
                                <li class="nav-item">
                                    <a href="{{ path('app_logout') }}" class="nav-link lien-nav">Déconnexion</a>
                                </li>
                                <li class="nav-item">
                                    <a href="{{ path('app_session') }}" class="nav-link lien-nav">Accueil</a>
                                </li>
                                <li class="nav-item">
                                    <a href="{{ path('app_stagiaire') }}" class="nav-link lien-nav">Liste stagiaire</a>
                                </li>
                                <li class="nav-item">
                                    <a href="{{ path('app_section') }}" class="nav-link lien-nav">Liste modules</a>
                                </li>
                            </ul>
                        </nav>
                    </div>
                </div>
                {% else %}
                <div class="container-fluid">
                    <a class="lien-nav nav-link" href="{{ path('app_register') }}">S'inscrire</a>
                    <a class="lien-nav nav-link" href="{{ path('app_login') }}">Se connecter</a>
                </div>
                {% endif %}
            </div>
        </div>
          
        {# <div class="nav-bar">
            {% if app.user %}
            <div class="nav">
                    <div class="lien">
                        <a class="lien-nav" href="{{ path('app_logout') }}">Déconnexion</a>
                        <a class="lien-nav" href="{{ path('app_session') }}">Accueil</a>
                        <a class="lien-nav" href="{{ path('app_stagiaire') }}">Liste stagiaire</a>
                        <a class="lien-nav" href="{{ path('app_section') }}">Liste modules</a>
                    </div>
                    <div class="pseudo">
                        <p class="lien-nav">{{ app.user.pseudo ~ ' '}}<i class="fa-solid fa-user"></i></p> 
                    </div>
            </div>
            {% else %}
            <div class="connection">
                <a class="lien-nav" href="{{ path('app_register') }}">S'inscrire</a>
                <a class="lien-nav" href="{{ path('app_login') }}">Se connecter</a>
            </div>
            {% endif %}
        </div> #}


        {% block body %}{% endblock %}

        {% block javascripts %}
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.min.js" integrity="sha384-BBtl+eGJRgqQAUMxJ7pMwbEyER4l1g+O15P+16Ep7Q9Q+zqX6gSbd85u4mG4QzX+" crossorigin="anonymous"></script>
        <script src="{{ asset('js/script.js') }}"></script>
        {% endblock %}
    </body>
</html>
